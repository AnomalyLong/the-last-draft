{
  "name": "Negative control: does waitFor on a bogus attribute value actually FAIL?",
  "steps": [
    {
      "action": "waitFor",
      "selector": "[data-testid='game-root'][data-home-team='BOGUS_TEAM_XYZ']",
      "timeout": 3000,
      "_comment": "MUST FAIL. If this reports OK, attribute waitFor is not a real assertion."
    }
  ]
}
