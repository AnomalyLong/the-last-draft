// Injected by Vite `define` (see vite.config.ts) into both the client and
// server bundles. Used by the `version` debug command.
declare const __BUILD_VERSION__: string;
declare const __BUILD_TIME__: string;
